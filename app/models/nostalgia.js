var Base = require('./base');

module.exports = Base.extend({
  defaults: {
    id: 'nostalgia',
    description: null,
    created_at: null,
    animations: [],

  },
});

module.exports.id = 'Nostalgia';
